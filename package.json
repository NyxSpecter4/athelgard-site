{
  "name": "athelgard-site",
  "version": "1.0.0",
  "description": "Athelgard — AI Coding Agent Platform",
  "private": true,
  "scripts": {
    "test": "echo 'No tests yet' && exit 0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
